# Core
torch>=2.0
transformers>=4.30
numpy
pandas
scikit-learn
scipy
tqdm

# Hyperparameter search
optuna>=3.0

# Plotting
matplotlib

# Kurdish morphological pipeline (pin: the coverage/preprocessing numbers in the
# paper were produced with this version)
klpt==0.1.7

# Almost-Stochastic-Order test, run inside test_evaluation_v2. The PyPI
# distribution is named deepsig, not deep-significance; the import is
# `from deepsig import aso`. test_evaluation_v2 degrades gracefully if it is
# absent (HAVE_ASO = False) and reports the other tests regardless.
deepsig
